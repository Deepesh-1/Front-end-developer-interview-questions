Difference between useState and useRef

I use useState for values that affect the UI, such as 
 - form values
 - API data,
 - loading states,
 - or modal visibility.
 
I use useRef for values that need to persist without affecting rendering, such as 
- DOM references,
- timer IDs,
- previous values,
- or third-party instances."
